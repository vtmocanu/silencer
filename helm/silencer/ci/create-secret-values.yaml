# `secrets.mode=create` — chart owns the Secret. Tokens are fake and only
# exercise the templating path; they never reach a cluster.
secrets:
  mode: create
  create:
    appToken: xapp-1-test-fake-token
    botToken: xoxb-test-fake-token
